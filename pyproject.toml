[project]
name = "multi-agent-red-team"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Guy", email = "guy.nutman@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.13.4",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.2",
]

[tool.uv]
# Flat layout: modules live at the repo root, so there is nothing to build.
package = false
