[project]
name = "multi-agent-red-team"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Guy", email = "guy.nutman@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.13.4",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.2",
]

[project.scripts]
multi-agent-red-team = "multi_agent_red_team:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
